# 隐私说明

[English](PRIVACY.md) · [简体中文](PRIVACY.zh-Hans.md) · [繁體中文](PRIVACY.zh-Hant.md)

最后更新：2026-08-23

公开隐私政策见
[https://my-tesla.app/privacy/](https://my-tesla.app/privacy/)。
本文以实际使用方式说明自托管数据路径。

## 车辆数据

使用自托管 TeslaMate 连接时，My T 从用户设置的服务器地址读取车辆信息。车辆历史仍保存在用户自己的 TeslaMate PostgreSQL 数据库中；My T 不会把这些历史数据转存到开发者运营的车辆数据库。

使用 Tessie 连接时，数据访问受用户的 Tessie 账号及 Tessie 服务条款约束。

## 凭据

服务器地址及认证凭据使用 iOS 安全设施保存，其中秘密信息储存在 Keychain。My T 不会要求用户提供 Tesla 账号密码。

只有用户明确开启 iCloud 配置同步时，连接配置和凭据副本才可能通过用户自己的私有 iCloud 账号同步，以便其他设备恢复设置。车辆行程、充电、GPS 历史和 TeslaMate 数据库内容不会复制到该配置备份。

用户导出 `.mytconnection` 文件或明确复制 HostBox 连接包时，iOS 只会通过用户选择的分享目标或粘贴操作传输所选连接数据。URL 交接不会携带密码、Bearer Token 或 Cloudflare Secret。连接文件可能包含用户选择分享的凭据，应当像密码一样妥善保管；My T 会先测试导入的端点，再将其保存到 Keychain。

## 可选 My T Companion

My T Companion 运行在用户自己的 TeslaMate 主机上。它以只读方式读取现有 PostgreSQL 数据库，并通过用户自己的安全端点把所请求的数据直接返回给 My T。它不会复制完整的 TeslaMate 车辆历史。

为了保留 iOS 之后无法还原的事件，Companion 会在自己的数据卷中保存容量受限的停车事件日志（默认最多保留最新 50,000 条），内容包括已观察到的休眠／唤醒、插枪／充电、安全／开闭状态和空调状态变化。所有这些数据均保留在用户自己的 VPS 上。

## 可选车辆软件通知

用户在兼容的 My T 版本中开启车辆软件通知后，Apple 推送通知服务需要使用由 App 运营的投递中继。中继会保存用于定位该 App 安装的 APNs 设备令牌和不透明安装标识符，以及可选的充电／导航 push-to-start 令牌、各实时活动会话的更新令牌、语言和创建／最近活动时间戳。

用户的 My T Companion 只发送经过签名的软件更新事件：不透明安装 ID、TeslaMate 车辆 ID 或显示名称、报告的更新类型／版本和观察时间。它**不会**发送 VIN、位置、TeslaMate 凭据、数据库密码、电池数据、路线、充电历史或行驶历史。

## 可选充电实时活动

兼容的 My T 版本与 My T Companion 1.10.0 或更高版本配合使用时，同一个投递中继可以在 App 未打开时启动并更新锁定屏幕或灵动岛上的充电实时活动。最小化的签名事件可包含不透明安装 ID、车辆 ID 或显示名称、充电会话 ID、开始／当前／目标电量百分比、车辆真实上报的额定续航及续航增加值、充电功率、剩余时长和预计完成时间。

此路径**不会**发送 VIN、位置、路线、TeslaMate 凭据、数据库密码、行驶历史或 kWh。缺失的续航数值会直接省略，不会估算。APNs 和实时活动令牌只用于定位用户自己的 App 安装。

## 可选导航实时活动

目的地导航进行期间，最小化的签名事件可包含不透明安装 ID、车辆 ID 或显示名称、导航会话 ID、目的地名称、剩余距离和分钟数、预计到达时间、预计到达电量百分比、已行驶／总距离，以及行驶轨迹是否已由 TeslaMate 验证。它**不会**包含 GPS 坐标、完整路线、VIN、TeslaMate 凭据或数据库密码。

## 推送中继保留策略

同一个 APNs 设备令牌再次注册时，中继会复用原安装身份并删除历史重复项；默认会删除连续 365 天没有重新注册或签名事件的安装记录，清理每 24 小时运行一次。车辆事件内容只用于即时 APNs 投递，不会由中继持久保存。旧版待投递负载会在每日维护时清除；实时活动成功结束后，该会话的更新令牌也会被删除。

投递审计日志最多保留 90 天，只包含不透明安装 ID、事件类型、投递结果／详情和时间戳。它不会保留车辆名称或 ID、目的地、电池数值、软件版本或事件内容摘要。更新本政策时，旧版审计行已经过脱敏处理。

每个 App 安装使用独立秘密和签名请求。关闭通知功能不会影响停车、导航或其他自托管功能。此通知路径与常规车辆历史访问相互独立；后者仍然由用户的服务器与 My T 直接通信。

## 可选“锁车且车内无人”通知与声音

开启后，Companion 可以发送报告指定车辆被观察到已上锁且车内无人的最小签名事件。通知包含用户可见的标题和消息。声音在每台 iPhone 上分别选择：内置声音标识符、导入的音频、导入文件名和静音选择都不会离开该设备。导入文件会转换后储存在 App 的私有通知声音目录，用户可以自行删除。

如果移除注册时暂时无法连接中继，My T 只会保留本机重试标记并稍后重试；这不会保留车辆事件内容，也不会重新开启通知。

## 公开支持

切勿在公开 GitHub Issue 中提交凭据、服务器地址、VIN、GPS 坐标、`.env` 文件、数据库导出、含私人位置的截图或原始生产日志。

关于 App Store 隐私披露的问题，请使用
[官方支持页面](https://my-tesla.app/support/)。
